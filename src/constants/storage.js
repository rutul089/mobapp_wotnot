const mm = 'WotNot';
const prefix = `@${mm}:`;

export const EXISTING_USER = `${prefix}existingUser`;
export const REFRESH_TOKEN = `${prefix}refreshToken`;
export const AUTH_TOKEN = `${prefix}authToken`;
export const IS_LOGIN = `${prefix}isLogin`;
export const USER_PROFILE = `${prefix}userProfile`;
export const USER_DETAIL = `${prefix}userDetail`;
